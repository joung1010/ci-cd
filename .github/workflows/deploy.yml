name: Github Actions Test

on:
  push:
    branches:
      - main


jobs:
  My-Deloy-Jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        run: echo "Hello World"
      - name: Step check1
        run: |
          echo "Good"
          echo "Hello"
